{
  "name": "spring-splosion-task-master",
  "version": "1.0.0",
  "description": "A class which when an instance is inititiated, runs a takes in a string and manipulates it to return a sploded version of it",
  "main": "springSplosion.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ope oyewole",
  "license": "ISC"
}
